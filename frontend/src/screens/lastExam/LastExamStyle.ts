import { StatusBar, StyleSheet } from "react-native";

const LastExamStyle = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: StatusBar.currentHeight,
    justifyContent: "center",
    alignItems: "center",
  },
});
